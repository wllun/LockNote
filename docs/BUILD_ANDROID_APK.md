# Build Android APK

Run this command from the LockNote project directory:

```powershell
eas build --platform android --profile preview
```

When the build finishes, open the link provided by EAS on your Android device to download and install the APK.
